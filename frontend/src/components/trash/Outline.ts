/*
export class SubTitle{
  subtitleIndex:number;       //标题总索引
  subtitleName:string;        //标题名称
  subtitleLevel:number;       //标题级别
  subtitleLevelIndex:number;  //标题级别下索引
  parentIndex:number;         //父级标题索引
  constructor(name: string, index: number,level:number,levelIndex:number,parentIndex:number) {
    this.subtitleName = name;
    this.subtitleIndex = index;
    this.subtitleLevel = level;
    this.subtitleLevelIndex = levelIndex;
    this.parentIndex = parentIndex;
  }
}

*/
