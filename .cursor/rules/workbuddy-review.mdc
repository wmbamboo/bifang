---
description: 每次代码改动后向 WorkBuddy 交接审查摘要
alwaysApply: true
---

# WorkBuddy 审查交接（毕方仓库）

每当本轮会话完成了代码修改（编辑 / 新建 / 删除），**在会话结束前**把改动摘要**追加**（禁止覆盖）写入项目根目录的 `.workbuddy-handoff.md`。只读不改的会话不需要追加。

格式（每次一个 section）：

```
## <ISO 日期时间> <一句话主题>
- 基线: <本段改动开始前的 HEAD 短 hash，即运行 git rev-parse --short HEAD 的输出；写在本段第一行>
- 改动文件: <相对路径列表>
- 动机: <要修的报错/需求，如「大纲填充报错：数值不忠实 3.3亿」>
- 关键决策: <为什么这样改，放弃了什么替代方案>
- 风险点: <可能影响的调用方、边界条件、回归面>
```

注意事项：
- 「基线」必须在动手改代码**之前**先取好（git rev-parse --short HEAD），这样 WorkBuddy 审查时用 `git diff <基线>` 精确圈定本段改动，与工作区里其他未提交改动区分开。
- 摘要写「决策与风险」，不要贴大段代码；diff 由 WorkBuddy 侧用 git 自取。
- 一次会话多个不相关改动，分成多个 section。
- WorkBuddy 侧的自动审查会读取本文件 + git diff 做复核，缺失交接段时审查会以 diff 为准并标注「无交接摘要」。
