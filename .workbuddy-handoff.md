## 2026-09-25T10:21+08:00 真 table/image_grid 模板与灌模贯通
- 改动文件: `backend/scripts/build_table_image_grid_slides.py`, `frontend/public/pptTemplate-simple.pptx`, `frontend/src/components/DocUtil/template-manifest.json`, `frontend/public/template-manifest.json`, `frontend/src/components/DocUtil/templateManifest.ts`, `frontend/src/components/DocUtil/PptTemplate.ts`, `frontend/src/components/DocUtil/outlineJson.ts`, `frontend/src/components/DocUtil/ViewItem4Ppt.ts`, `frontend/src/pages/AiOutlineGenPpt.tsx`, `frontend/src/pages/KbOutlineGenPpt.tsx`, `frontend/src/components/DocUtil/OutlineTreeEditor.tsx`, `frontend/src/components/DocUtil/outlineSerialize.ts`, `frontend/src/components/ChatUtil/OutlinePromptComposer.tsx`, `req/ppt设计评审-改进建议.md`, `req/生成PPT质量评审与技术改进指导.md`
- 动机: 设计评审「真 table/image_grid」待办；原先枚举存在但渲染降级 list
- 关键决策: 占用保留页 42/43 写 5×4 表与 2×2 图鉴占位；manifest 直指页码；去掉 `layoutForRender` 降级；新增 `buildTableFillVars`/`buildImageGridFillVars`。图鉴暂用模板内 jpeg，未做 PDF asset 换图
- 风险点: 前端需刷新才能吃到新 pptx；image_grid 仍是占位图；`reserved` 现为 44–46

## 2026-09-25T10:21+08:00 分栏 colSub 同文不再整页拒收
- 改动文件: `frontend/src/components/DocUtil/outlineEvidenceValidate.ts`, `frontend/src/components/ChatUtil/OutlinePromptComposer.tsx`, `frontend/src/components/DocUtil/outlineJson.ts`（`dropDuplicateColSub`）
- 动机: 大纲填充失败「colSub: 头部档量级」被当成同文双卡
- 关键决策: `findDuplicateTips` 跳过 colSub/副标；重复副标改为保首个删除其余（修复而非拒收）；提示词正例不再写同一句副标
- 风险点: 真同文正文短条目仍拒；副标弱重复依赖 drop 路径在 parse 时执行

## 2026-09-25T10:21+08:00 数值忠实度闸：多 token / intent / 就近实体 / 邻块 / 分栏继承
- 改动文件: `frontend/src/components/DocUtil/outlineEvidenceValidate.ts`, `frontend/src/components/DocUtil/outlineJson.ts`, `frontend/src/components/DocUtil/pptEval.ts`, `frontend/src/components/ChatUtil/ChatWithSpeech4Ppt.tsx`, `frontend/src/components/ChatUtil/OutlinePromptComposer.tsx`
- 动机: 连续误杀/漏杀——大盘 7280.1万 串品类页、polo 403.7万 / 衬衫 3.3亿 远距误杀、合刊混合 chunk 洗白、裸同比+30.6% 掉栏、TOP6 改写品类总销量
- 关键决策:
  - 逐 token 校验；tip 内就近口径；`intent` 选型声明 + 标题降级回填
  - CHUNK_SCOPE：就近实体类（平距并列）+ 数字块零实体时邻块拼接（表头优先不拼下一块）
  - 实体词表单文件导出；TOP 闸扩到 category-detail 等 intent
  - columns：`col:` 后条目继承栏口径；无口径裸同比直接拒并给重试提示
  - `pptEval.runFidelityGoldCases` 固化多案；timing 记 `fidelityRejects`/`fidelityRecovered`
  - 注释约定：表头分家类误杀再 ≥2 次应停启发式转 JSON 双轨
- 风险点: 窗口/就近类仍是过渡方案，合刊 OCR 边界 case 可能继续冒；intent 依赖选型模型配合；邻块拼接可能在极端邻块布局下仍偏；调用方须传 evidence 且 chunk 标记 `⟦chunk:…⟧` 才吃满能力

## 2026-09-25T10:55+08:00 TOP 闸方案 B + 价格带口径 + 金标 CI
- 基线: e977d8e
- 改动文件: `frontend/src/components/DocUtil/outlineJson.ts`, `frontend/src/components/DocUtil/outlineEvidenceValidate.ts`, `frontend/src/components/DocUtil/pptEval.ts`, `frontend/src/components/ChatUtil/OutlinePromptComposer.tsx`, `frontend/tests/fidelityGold.test.ts`, `frontend/jest.config.js`, `frontend/public/pptTemplate-simple-workbuddy.pptx`（删除）
- 动机: 误杀代价（大纲生成中断）> 漏杀代价（rank 低危瑕疵）；价格带 tip「￥50-100 销量」被裸数字兜底误杀；Watch 项落地
- 关键决策:
  - TOP 闸收窄为只拦「TOP N + 合计/共计/总和」；裸「销量TOP6 403.7万」放行（方案 B）
  - 金标拆分：`top-sum-rewrite-block` 须拦 + `top-native-rank-pass` 须过；新增价格带正/反各一案
  - 口径识别加 `PRICE_BAND_TOKEN_RE`；price-band tip 跳过衬衫/polo/大盘就近对齐
  - 提示词：`col:` 轴名须含品类/大盘词；删已取代的 workbuddy pptx
  - `jest.config.js`（ts-jest）+ `tests/fidelityGold.test.ts` 挂 `runFidelityGoldCases`（14 案全过）
  - handoff 基线规则已在 `.cursor/rules/workbuddy-review.mdc` / `.cursorrules`，本段起执行
- 风险点: rank 错写（TOP7→TOP6）暂不拦，等 JSON 双轨 rank_badge；价格带跳过实体对齐后串窗只靠证据存在性；旧 tests 本就因无 jest 配置失败，现配置可跑 fidelityGold，其它套件未必全绿

## 2026-09-25T11:40+08:00 JSON 抽取彻底改善（版面归属四层）
- 基线: e977d8e
- 改动文件: `backend/app/services/table_structure.py`, `backend/app/services/extract_service.py`, `backend/app/services/extract_labeler.py`, `backend/app/services/document_loader.py`, `backend/app/services/upload_task_service.py`, `backend/app/services/kb_service.py`, `backend/tests/test_extract_gold.py`, `backend/tests/fixtures/polo_p{2,4,6}.md`, `backend/scripts/backfill_extracts.py`, `backend/requirements.txt`, `backend/README.md`, `req/生成PPT质量评审与技术改进指导.md`, `req/000-ppt大纲生成改进设计.md`；回填 `服装/extracts|parses/抖音单品爆款分析-商务男士衬衫polo衫.*`
- 动机: 文本流拍平导致 yoy 串行、指标名「指标」、表被 500 字切开、双轨不同源、240 截断；落实「归属来自版面坐标」
- 关键决策:
  - ① snippet 放宽至 4000；② HTML/MD 表整块 atomic chunk；③ row/cell 抽指标，增速只绑同格/配对格，产出 rank_badge；④ LLM 仅贴标签且 raw_text 校验（默认关）；⑤ pytest 金标 P2/P4/P6；⑥ 同一次 text → parses + extracts + vectors（extract 不用碎 pieces）
  - 像素级 VL bbox 暂用 table_grid 行列坐标；属性图页无数字 → extract_warnings 而非静默当成功
- 风险点: 已入库 chunk 仍是旧切开表，需重向量化才吃 atomic；价格带行部分仍名「指标」可走 labeler；P6 读图抽数未做

## 2026-09-25T13:05+08:00 附属占比% 不再被表内总销量抢就近
- 基线: e977d8e
- 改动文件: `frontend/src/components/DocUtil/outlineEvidenceValidate.ts`, `frontend/src/components/DocUtil/pptEval.ts`
- 动机: 大纲填充失败「403.7万 polo…占比5.5%」——5.5% 在证据表里被总销量/大盘抢最近实体
- 关键决策: tip 已点名品类/大盘且同条含万/亿主量时，占比/同比/环比 % 只验证据存在（`isCompanionRateToken`），不单独 CHUNK_SCOPE；金标 `polo-share-companion-rate`
- 风险点: 恶意「polo+错占比」且该 % 恰在证据中会出现漏拦；主量万/亿仍走实体对齐把关

## 2026-09-25T23:59+08:00 价格带 tip 认 ¥50以下 / table 管道行
- 基线: e977d8e
- 改动文件: `frontend/src/components/DocUtil/outlineEvidenceValidate.ts`, `frontend/src/components/DocUtil/pptEval.ts`
- 动机: table 页「¥50以下|126.5万|…」被裸数字拒——PRICE_BAND_TOKEN_RE 只认区间不认「以下/以上」
- 关键决策: 扩展价格带 token（以下/以上）；price-band intent 下 tip 含 ￥/¥+以下|以上|区间 则只验证据存在；金标 `price-band-below-pipe-ok`
- 风险点: 价格带页仍要求 tip 点名价区；裸同比继续拒

## 2026-09-26T00:20+08:00 抽取残留三缺口：行头绑定 / 属性纯文本 / product_grid
- 基线: 696c558
- 改动文件: `backend/app/services/table_structure.py`, `backend/app/services/extract_service.py`, `backend/tests/test_extract_gold.py`, `backend/tests/fixtures/polo_p6.md`, `backend/tests/fixtures/polo_p9.md`, `.github/workflows/ci.yml`
- 动机: 占位名「指标」过多；P6 误诊需 VL；P9/P16 图鉴误判 price_band
- 关键决策:
  - 同行最左文本/价带格 → name + price_band；量值格抽 share；后继纯%合并为 yoy（不再独立成条）
  - 属性页：`棉 72.18%` 类纯文本抽取；warning 改为「OCR 未检出标签+占比」而非需 VL
  - 新增 page_type=`product_grid`，caption「¥x 本期销量」抽取；爆款/TREND 优先于 price_band
  - 金标扩到 8 案；CI `backend-smoke` 挂 `pytest tests/test_extract_gold.py`
  - P6 夹具补入应有属性文本（当前库内 OCR 仍缺字，真库需重向量化才有 棉/纯色）
- 风险点: 榜单「商品名|7357」跨文件表结构未统一处理；P6 实库在重 OCR 前仍 metrics=0 但 warning 文案已纠正

## 2026-09-26T00:25+08:00 行头绑定通用化（页型 schema + 三信号 + LLM 兜底 + profile）
- 基线: 696c558
- 改动文件: `backend/app/services/table_profile.py`（新）, `backend/app/services/table_structure.py`, `backend/app/services/extract_service.py`, `backend/app/services/extract_labeler.py`, `backend/tests/test_table_profile.py`（新）, `backend/tests/fixtures/top_header_table.md`, `backend/tests/fixtures/double_header_table.md`；回填 `服装/extracts/抖音单品爆款分析-商务男士衬衫polo衫.jsonl`
- 动机: 「最左=行头」文档特判不可泛化；占位名需按表语法判定，歧义交 LLM 角色标注
- 关键决策:
  - 第2层页型 schema 优先（kpi/price_band/ranking/product_grid）；混页表内价带≥2 行覆盖 category_kpi→price_band
  - 第1层 general：th/密度/语义三信号投票 → TableProfile（header_axis/header_rows/row_header_cols）
  - 第3层 `label_table_cell_roles`（默认关，`label_ambiguous_tables`）；只标角色，value 仍 raw_text 校验
  - 页记录落 `table_profiles` + tables[].profile；金标扩上表头+双层 thead
  - polo 回填后 metrics=117 anon=1/117；P2 profile=page_schema:price_band
- 风险点: 双层表头仍可能 ambiguous（需显式开 LLM）；页型误判仍会短路信号投票；MD 无 sep 时首行不下 is_th

## 2026-09-26T00:36+08:00 审查三修：无标签速率 / CI 全测 / 250% 重复
- 基线: 696c558
- 改动文件: `backend/app/services/table_structure.py`, `backend/app/services/extract_service.py`, `backend/tests/test_extract_gold.py`, `.github/workflows/ci.yml`；回填 `服装/extracts/抖音单品爆款分析-商务男士衬衫polo衫.jsonl`
- 动机: 审查指出裸%被默认绑成 yoy 并在 snippet 补写「同比」=语义伪造；CI 漏挂 table_profile；P2 脚注「环比提升均达250%」被属性抽取成 value+share 双写
- 关键决策:
  - 邻格速率按列头/格内明文分类（yoy/mom/share/unlabeled）；无标签落 `rate_unlabeled`；rowspan 续行用视觉列偏移对齐列头；format/pseudo_docs 对 unlabeled 禁止补「同比」前缀
  - 有「销量同比」列头的 P2 仍正确为 yoy（非伪造）；无列头夹具断言必须 unlabeled
  - `metrics_from_attribute_prose` 仅 attribute 页启用，并滤同比/环比散文碎片；去掉属性指标上重复的 share=value
  - CI：`pytest tests/`（含 table_profile 上/双层表头）
- 风险点: rowspan 偏移启发式在非常规缺右列时可能错位；列头无「同比」字样但业务默认同比的表会变 unlabeled（宁缺勿伪）

## 2026-09-26T00:48+08:00 复合 tip「衬衫销量+占大盘%」主量不被大盘抢走
- 基线: 93bbefe
- 改动文件: `frontend/src/components/DocUtil/outlineEvidenceValidate.ts`, `frontend/src/components/DocUtil/pptEval.ts`
- 动机: 填充失败「296.2万 男士衬衫…占大盘4.1%」——点名大盘但数字最近实体是衬衫
- 关键决策: `entitiesClaimedNearNumberInTip` 对 after 窗改用最近实体（firstIndex），before 仍用 lastIndex；禁止远处「占大盘」抢主量口径。4.1% 仍走 companion rate。金标 `shirt-share-of-macro-compound-tip`
- 风险点: tip 写成「大盘视角下的衬衫296.2」且大盘词更靠近数字时会归大盘（符合就近原则）

## 2026-09-26T00:56+08:00 采样窗日期不再当指标校验
- 基线: 93bbefe
- 改动文件: `frontend/src/components/DocUtil/outlineEvidenceValidate.ts`, `frontend/src/components/DocUtil/pptEval.ts`
- 动机: columns 页 `colSub: 采样窗2024.03.19-04.17` 被抽成「2024.03」做实体对齐，在「col:大盘」下误报串用衬衫
- 关键决策: `isDateLikeNumberToken` / `isSamplingWindowTip`——年份与日期片段不进 extractMetricTokens；采样窗 tip 整段跳过忠实度。金标 `colsub-sampling-window-not-metric`
- 风险点: tip 写「采样窗内销量 296.2万」仍会抽 296.2万（正确）；纯「3.19」无采样语境仍可能当数（刻意收窄）

## 2026-09-26T01:02+08:00 补 Q1 2024 金标并提交推送前端闸门
- 基线: 93bbefe
- 改动文件: `frontend/src/components/DocUtil/outlineEvidenceValidate.ts`, `frontend/src/components/DocUtil/pptEval.ts`, `.workbuddy-handoff.md`
- 动机: 审查确认 00:48/00:56 可合入；提醒未提交未推送致 CI 未跑；顺手锁「Q1 2024」年份豁免
- 关键决策: 新增金标 `q1-2024-year-not-metric`；本段提交并 push main 以触发远端 CI；品类词受控化列为中期不落地
- 风险点: push 后需看 backend-smoke / frontend 金标 job

## 2026-09-26T01:10+08:00 成品闸误拦模板目录装饰序号致下载失败
- 基线: 8347f9e
- 改动文件: `frontend/src/components/DocUtil/PptProductGate.ts`, `frontend/src/components/DocUtil/PptTemplate.ts`, `frontend/src/pages/KbOutlineGenPpt.tsx`, `frontend/src/pages/AiOutlineGenPpt.tsx`
- 动机: 点「文档下载」失败——成品闸把模板 catalog 页装饰「01/02/03」判成孤立数字碎片
- 关键决策: `isOrphanNumberFragment` 豁免零填充装饰序号、年份与日期片段；仍拦裸「30」类截断；saveDoc try/catch 回显真实错误；缺模板页抛可读异常
- 风险点: 真·截断 tip 若写成「01」会被放过（极少）；未灌槽仍由占位符残留闸拦截

## 2026-09-26T01:15+08:00 拒收「材料未覆盖/口径未标注」类元话语 tip
- 基线: 8347f9e
- 改动文件: `frontend/src/components/DocUtil/outlineJson.ts`, `frontend/src/components/ChatUtil/OutlinePromptComposer.tsx`, `frontend/src/components/ChatUtil/ChatWithSpeech4Ppt.tsx`, `frontend/src/components/DocUtil/ViewItem4Ppt.ts`
- 动机: 「款式与卖点筛选动作」分栏把检索诊断（材料未覆盖、仅见一项指标、口径未标注无法定位）写成 tip/生成内容
- 关键决策: `findMetaDiagnosticTips` 填充校验拒收；columns 契约与写作格式禁元话语，材料薄时改写可执行选品短动作；内容校验同步拦
- 风险点: 页题若本就不贴材料（动作页无属性/图鉴证据），会反复重试——宜改页题为「属性特征筛选」或拆到有证据的章

## 2026-09-26T01:16+08:00 指标旁注采样日期日碎片不再当指标
- 基线: 8347f9e
- 改动文件: `frontend/src/components/DocUtil/outlineEvidenceValidate.ts`, `frontend/src/components/DocUtil/pptEval.ts`
- 动机: `metric: …7280.1万（2024.03.19-04.17）` 把日「19」抽成指标，点名大盘却就近 polo/衬衫
- 关键决策: `numberInsideDateSpan`——落在 tip 内日期/区间跨度的数字一律豁免；金标 `metric-with-date-paren-not-day-fragment`
- 风险点: 真指标若写成「销量19万」且紧贴 `2024.xx` 日期串可能误豁免（罕见；有万单位仍保留）

## 2026-09-26T01:25+08:00 元话语前置到结构页题 + 分栏轴证据闸 + 常量归一
- 基线: d7e01e8
- 改动文件: `frontend/src/components/DocUtil/outlineMetaDiagnostic.ts`（新）, `outlineCoverage.ts`, `outlineJson.ts`, `PptProductGate.ts`, `OutlinePromptComposer.tsx`, `ChatWithSpeech4Ppt.tsx`, `ViewItem4Ppt.ts`, `tests/outlineMetaCoverage.test.ts`
- 动机: 审查指出元话语拒收只在填充后，动作页题无素材会反复重试；词表复制 5 处；目录序号「10」成品闸仍可能误杀
- 关键决策:
  - 结构阶段 `findUnsupportedActionSlideTitles`：筛选动作类页题未点名属性/面料/图案/价格带/图鉴等 → 拒收换页题（tips 本不在结构 JSON，用页题风险闸等价前置）
  - 填充阶段 `checkColumnAxisEvidence`：按 col 轴对证据做栏级覆盖；元话语/栏轴失败纳入降 list 条件
  - 词表抽到 `outlineMetaDiagnostic.ts` 单点导出；提示词/重试/校验共用
  - 成品闸目录序号豁免扩到 1～9、10～18（仍拦裸 30）
- 风险点: 「款式与卖点筛选动作」会被结构拒——须改成「…属性筛选」；栏轴启发式对冷门轴名可能过严而降 list

## 2026-09-26T01:39:55+08:00 价格带 ¥50-100 边界数字忠实度误拦
- 基线: 58432e4
- 改动文件: frontend/src/components/DocUtil/outlineEvidenceValidate.ts, frontend/src/components/DocUtil/pptEval.ts, frontend/tests/priceBandTokenFidelity.test.ts
- 动机: 大纲填充失败：表 tip「polo衫|¥50-100|172.6万|+17.09%|1.31亿」中边界「100」被当独立指标，证据无裸 100 即拦
- 关键决策: 对齐日期跨度做法——`isPriceBandBoundToken` + `PRICE_BAND_SPAN_RE`，抽 token 时跳过落在 `PRICE_BAND_TOKEN_RE` 跨度内的无万/亿/% 数字；真指标（172.6万/1.31亿）仍验。放弃放宽 evidenceHasMetric 对「证据含 ¥50-100 即算有 100」——那掩盖编造、且用户证据本就无 100
- 风险点: 若 tip 把真指标写成与价带同形且无单位（极少），会被跳过；`50-100` 无 ￥/¥/元 后缀仍不识别为价带（与既有 PRICE_BAND_TOKEN_RE 一致）

## 2026-09-26T07:58:00+08:00 修复 frontend 金标被 gitignore 吃掉 + columns 正例对齐栏轴闸
- 基线: 58432e4
- 改动文件: frontend/.gitignore, frontend/jest.config.js, frontend/tests/, frontend/package-lock.json, .github/workflows/ci.yml, frontend/src/components/ChatUtil/OutlinePromptComposer.tsx, frontend/src/components/DocUtil/outlineEvidenceValidate.ts, frontend/src/components/DocUtil/pptEval.ts, frontend/tests/priceBandTokenFidelity.test.ts, .workbuddy-handoff.md
- 动机: 审查 P0——`.gitignore` 误忽略 `/tests/`、`/jest.config.js`（及误粘贴 `/hezl/`），远端 CI 金标空转；顺手修 columns 正例与 `checkColumnAxisEvidence` 互斥；纳入上一轮价带边界数字误拦修复
- 关键决策: 清掉误 ignore，并取消忽略 `package-lock.json`（否则 `npm ci` 仍挂）；遗留 ViewItem/Template 用例因 nanoid ESM/空壳失败，用 `testPathIgnorePatterns` 排除而非硬塞绿；CI 金标步扩跑 meta/价带三套；columns 正例改为衬衫/polo/大盘域内轴，营销口号改反例。轴词表受控化（中风险2）本轮不收拢，记中期
- 风险点: 首次纳入 ~2MB package-lock；遗留测试被 ignore 后「全量」实际=闸门三套，迁移遗留前勿删 ignore；换文档域仍受 outlineCoverage 硬编码轴词限制

## 2026-09-26T08:05:00+08:00 CI npm ci 跳过 postinstall
- 基线: ceb1f7c
- 改动文件: .github/workflows/ci.yml
- 动机: push 后 frontend-tests 在 `npm ci` 红（后端绿）；本地同 lockfile 可装，疑 postinstall `max setup`/prepare husky 在 Actions 环境失败，且无权限拉 job log
- 关键决策: `npm ci --ignore-scripts`——闸门测试不依赖 umi generate/husky；放弃先改成 npm install（锁文件应用 ci）
- 风险点: 若日后测试依赖 postinstall 生成物需再显式加一步

## 2026-09-26T08:13:00+08:00 CI npm ci：跳过 husky/max + 去掉 file: 自引用
- 基线: ebece9f
- 改动文件: .github/workflows/ci.yml, frontend/package.json, frontend/package-lock.json, .workbuddy-handoff.md
- 动机: `--ignore-scripts` 仍跑 prepare（`ant-design-pro: file:` 自链）；`husky install` 在 frontend 子目录报 `.git can't be found` → job 红
- 关键决策: CI 装依赖前 `npm pkg delete scripts.prepare/postinstall` 再 `npm ci`；package.json prepare 改为 try/catch 兜底；删除 `dependencies["ant-design-pro"]=file:`
- 风险点: Actions runner 上临时改 package.json 不影响仓库；本地仍会跑 max setup（postinstall 未改）
